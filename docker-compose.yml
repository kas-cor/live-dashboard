services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${DASHBOARD_HOST:-127.0.0.1}:${DASHBOARD_PORT:-3003}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /var/log:/host/log:ro
      - ${TODO_HOST_PATH}:/host/todo:ro
      - /run/tailscale:/var/run/tailscale:ro
      - ${HOME}/.ssh:/root/.ssh:ro
      - /projects/dashboard/data:/ollama-data:ro
      - ${HOME}/.hermes/data:/hermes-data:ro
      - dashboard-data:/data
    environment:
      - HOST_PROC=/host/proc
      - HOST_SYS=/host/sys
      - BACKEND_HOSTNAME=${BACKEND_HOSTNAME:-host}
      - PROMO_DB_PATH=/hermes-data/promotions.db
    cap_add:
      - SYS_PTRACE

volumes:
  dashboard-data:
