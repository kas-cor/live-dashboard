name: dashboard-proxy
services:
  nginx-dashboard:
    image: nginx:alpine
    container_name: nginx-dashboard
    network_mode: host
    volumes:
      - ./dashboard-proxy/conf/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
